class PortfolioModel {
  final int idx;
  final String name, desc, imageUrl;

  // code here
  // 생성자 만들기
  PortfolioModel(
      {required this.idx,
      required this.name,
      required this.desc,
      required this.imageUrl});
}