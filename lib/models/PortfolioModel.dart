class PortfolioModel {
  final int idx;
  final String name, desc, imageUrl;

  PortfolioModel(
      {required this.idx,
      required this.name,
      required this.desc,
      required this.imageUrl});
}
